export interface IconsProps {
  type: string;
  label: string;
  onPress: () => void;
  style: Object | any[];
  role: string;
  theme: Object;
  name: string;
  size: number;
  color: string;
  stroke: string;
  loading: boolean;
}
